# Tiến độ KHOAN ĐÃ

- 2026-07-23 — Theo phản hồi trực tiếp, giữ hero “Bác đang gặp chuyện đáng ngờ?” bằng HTML thật, khôi phục bảng màu Trust Blue và sửa ảnh ông bà bị lặp ở nửa phải. Giữ nguyên toàn bộ nâng cấp chức năng, bảo mật và thương hiệu KHOAN ĐÃ.
- 2026-07-23 — Tái định vị sản phẩm thành **KHOAN ĐÃ – Dừng lại trước khi chuyển tiền**; chuyển hệ token sang giấy ấm + xanh lá + cam, giữ đỏ cho nguy hiểm; bỏ hero bitmap có chữ baked-in trên desktop để toàn bộ nội dung là HTML thật.
- 2026-07-23 — Bổ sung nhập PNG/JPEG/WEBP/PDF kéo-thả, hủy phân tích, lớp tín hiệu/độ tin cậy, sửa nội dung AI đọc, Reputation Engine không bịa số báo cáo, lớp `public/services.js`, danh bạ hỗ trợ người thật và 11 bài luyện ngắn.
- 2026-07-23 — Mở rộng Family Circle với email/quyền/thời hạn/thu hồi/audit; Privacy Center có chia sẻ gia đình, retention, kiểm tra một lần và audit; luồng bị thúc ép thành 6 bước có TTS; cứu hộ sau chuyển tiền ghi thời gian/kênh/OTP.
- 2026-07-23 — Hardening: CSP/security headers, rate limit, MIME/size/base64/signature validation, PDF instruction, PWA manifest. Test thương hiệu, adapter, API, media và Reputation Engine; toàn bộ 80/80 test đạt và `npm audit --omit=dev` báo 0 lỗ hổng.


- 2026-07-21 01:15 (UTC+7) — Nhiệm vụ 1: Khởi tạo cấu trúc Node.js + Express tại `D:\LaChanSo`; giữ nguyên `.env` và `.gitignore`; workspace không có Git nên không commit.
- 2026-07-21 01:15 (UTC+7) — Nhiệm vụ 2: Hoàn thành rule engine deterministic, thư viện trích dẫn tĩnh và 10 fixture giả. Điều chỉnh ngưỡng “Nguy hiểm cao” từ 6 xuống 4 vì kịch bản giả người thân bắt buộc chỉ tạo 4 điểm hợp lệ (khẩn cấp + người thân đáng ngờ).
- 2026-07-21 01:22 (UTC+7) — Nhiệm vụ 3: Hoàn thành Gemini wrapper với JSON schema cố định 7 boolean, một lần gọi mỗi yêu cầu, timeout 30 giây và unit test không lộ key.
- 2026-07-21 01:22 (UTC+7) — Nhiệm vụ 4: Hoàn thành endpoint `POST /api/phan-tich`, kiểm tra input và ghép Gemini → rule engine; thêm endpoint health cho vận hành.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 5: Hoàn thành màn hình kiểm tra và kết quả 3 lý do + 3 hành động, chữ lớn, tương phản cao, responsive, điều hướng bàn phím và icon Lucide.
- 2026-07-23 — Thay icon chức năng bằng IconScout Unicons Line vendored cục bộ; làm phẳng nền icon để giao diện bớt cảm giác AI/3D và giữ nguyên lớp tương tác hiện có.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 6: Hoàn thành lưu số người thân trong localStorage và mở cuộc gọi bằng `tel:`.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 8: Hoàn thành Web Speech API để nhập giọng nói và SpeechSynthesis API để đọc kết quả tiếng Việt.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 9: Hoàn thành từ khóa an toàn gia đình lưu cục bộ và lời nhắc khi phát hiện người thân đáng ngờ.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 10: Hoàn thành màn hình lịch sử phụ, lưu tối đa 5 lần kiểm tra gần nhất.
- 2026-07-21 01:38 (UTC+7) — Nhiệm vụ 11: Hoàn thành cảnh báo “Nguy hiểm cao” toàn màn hình với hành động gọi người thân nổi bật.
- 2026-07-21 01:35 (UTC+7) — Nhiệm vụ 7, lượt E2E đầu: 9/10 fixture đạt qua giao diện thật. Sửa kỳ vọng fixture tự bổ sung `cong-an-moi-len-lam-viec` từ “Nghi ngờ” thành “An toàn thấp” vì số gọi đã lưu, chỉ mời lên trụ sở và không có yêu cầu tiền/mã nên không đủ bằng chứng giả danh.
- 2026-07-21 01:36 (UTC+7) — Nhiệm vụ 7, lượt E2E cuối: 10/10 fixture đạt qua ô nhập và nút “Phân tích” trên trình duyệt thật; lịch sử giữ đúng 5 mục gần nhất.
- 2026-07-21 01:37 (UTC+7) — Nhiệm vụ 3, kiểm thử thật: 3/3 tình huống console đạt với `gemini-3.1-flash-lite`; model `gemini-3.5-flash` được loại vì API trả 503 high demand tại thời điểm kiểm thử.
- 2026-07-21 01:40 (UTC+7) — Nhiệm vụ 12: Rà soát bảo mật và bàn giao hoàn tất; 23/23 test đạt, npm audit 0 lỗ hổng, không có key hardcode/dấu vết sản phẩm cũ, README và `.env.example` đã cập nhật, responsive đạt 320/375/414/768/1280/1440 px.
- 2026-07-21 01:41 (UTC+7) — Hoàn thiện quyền riêng tư: thêm thao tác xóa số người thân và mật khẩu gia đình khỏi localStorage; dọn dữ liệu giả sau E2E.

HOÀN THÀNH TOÀN BỘ DANH SÁCH NHIỆM VỤ

## Cập nhật sau bàn giao (phiên làm việc tiếp theo với Claude)

- 2026-07-21 — Làm lại giao diện: bảng màu ấm hơn (accent xanh teal thay vì xanh-xám lạnh), bọc form/kết quả trong workspace-card bo góc có đổ bóng, huy hiệu tròn cho logo, nhãn eyebrow dạng pill. Không đổi ID/class mà `app.js` phụ thuộc. `npm test` vẫn 23/23 pass, bao gồm test contrast WCAG tính lại từ giá trị màu mới.
- 2026-07-21 — Thêm tính năng đọc ảnh chụp tin nhắn (OCR qua Gemini vision, không dùng API OCR riêng): nút "Tải ảnh tin nhắn", nén ảnh (resize + JPEG) ngay trên trình duyệt trước khi gửi, backend nhận base64 tối đa ~5MB, Gemini đọc nội dung ảnh và trả thêm trường `noi_dung_da_doc` để người dùng đối chiếu OCR có đúng không. Mở rộng schema Gemini, thêm 2 test mới cho luồng ảnh (`gemini.test.js`), cập nhật test cũ theo schema mới. `npm test` 25/25 pass. Kiểm thử sống bằng ảnh PNG chứa văn bản kịch bản giả (tạo qua PowerShell System.Drawing, không dùng dữ liệu thật của ai) — Gemini đọc đúng 100% nội dung và phân loại đúng "Nguy hiểm cao". Ảnh không được lưu lại ở server hay localStorage, chỉ dùng để phân tích.
- 2026-07-21 — Làm lại bảng màu/bố cục lần 2 theo phản hồi "giao diện xấu": thêm banner khẩn cấp màu đỏ ngay đầu trang, link phụ "Kiểm tra trước khi chuyển tiền", dọn lại tiến trình node bị trùng (2 process cùng chạy do các lần restart trước không kill sạch — nguyên nhân khả dĩ của "lỗi" người dùng thấy, cộng thêm cache trình duyệt cũ).
- 2026-07-21 — Thêm 3 tính năng theo yêu cầu, đã cắt bớt phần không khả thi (dashboard con cháu từ xa, gửi tin nhắn tự động qua SMS/Zalo — cần backend tài khoản/API nhắn tin riêng, không làm kịp):
  1. **Chế độ khẩn cấp sau khi chuyển tiền** (`#vua-chuyen-tien`): checklist 6 bước tĩnh, có ô tick nhớ tiến độ (localStorage), nút gọi người thân, link sang nhật ký bằng chứng.
  2. **Kiểm tra trước khi chuyển tiền** (`#chuyen-khoan`, `POST /api/kiem-tra-chuyen-khoan`): bộ luật/rule-engine thứ hai (5 dấu hiệu bất thường chuyển khoản), hàm Gemini thứ hai `extractTransferSignals` dùng chung `callGemini` với luồng chính. Tái dùng `dangerDialog` khi kết quả "Nguy hiểm cao". Thêm `test/transfer.test.js` (4 test). `npm test` 29/29 pass.
  3. **Nhật ký bằng chứng** (`#bang-chung`): lưu số điện thoại/link/số tài khoản/thời gian/tóm tắt do người dùng tự nhập (không lưu ảnh gốc), xuất file `.txt` tải về, xóa từng mục. Lưu hoàn toàn trên `localStorage`, không có backend nào lưu trữ.

  Kiểm thử sống qua UI thật (không chỉ unit test): kiểm tra chuyển khoản với kịch bản "công an đòi nộp phạt vào tài khoản cá nhân" → đúng "Nguy hiểm cao", đúng 4 tín hiệu; checklist tick/gạch ngang/persist qua reload đúng; nhật ký bằng chứng lưu/hiển thị/xóa đúng qua localStorage thật.
- 2026-07-21 — Thêm kịch bản lừa đảo thứ 3: **"sở hữu kỳ nghỉ"** (đã tra cứu xác nhận là vấn nạn thật, 187 bị can/181 tỷ đồng thiệt hại tính đến 6/2026 — nguồn: Soha, Dân trí, VTV, Nhân Dân, Dân Việt, Tuổi Trẻ). Thêm 5 dấu hiệu mới vào rule-engine hiện có (mời hội thảo tặng quà, ép ký ngay giảm giá sốc, hợp đồng giá trị lớn nhiều năm, ép mua thêm hợp đồng để thoát hợp đồng cũ, gọi điện liên tục gây áp lực) — tái dùng nguyên luồng `/api/phan-tich`, không cần form/endpoint mới. Thêm 3 fixture, cập nhật danh sách trích dẫn cho phép trong test. `npm test` 32/32 pass, kiểm thử sống với Gemini xác nhận đúng.
- 2026-07-21 — **Làm lại toàn bộ giao diện** theo ảnh mẫu người dùng cung cấp: đổi bảng màu sang xanh dương (Trust Blue), logo hai màu (khiên + lá), thêm minh họa nhà/thành phố dạng flat-SVG, chuyển từ topbar-nav sang **thanh tab dưới nổi, bo tròn toàn phần** (Trang chủ / Kiểm tra / Lịch sử / Gia đình), thêm màn hình Trang chủ (hub 3 thẻ + banner khẩn cấp + banner "các hình thức lừa đảo"), thêm màn hình tĩnh "Các hình thức lừa đảo" (`#canh-bao`), thêm đồng hồ đếm ngược **thật** (không khóa thao tác) và nút "Gọi ngân hàng" trong dialog khẩn cấp, thêm trường số điện thoại ngân hàng vào cài đặt.
  - **Quyết định quan trọng cùng người dùng:** ảnh mẫu có tab "Gia đình" mô phỏng giám sát nhiều thành viên từ xa (cần tài khoản/backend/gửi thông báo — không khả thi trong thời gian còn lại). Đã hỏi và người dùng chọn: đổi tab này thành **"Cài đặt gia đình" trung thực** — chỉ hiển thị số điện thoại người thân/ngân hàng và mật khẩu gia đình thật sự đã lưu, không bịa dữ liệu Mẹ/Bố/Bà giả.
  - Bug tự phát hiện và sửa: dialog khẩn cấp full-screen ban đầu không cuộn được, có thể cắt mất nút "Gọi ngân hàng"/"Nghe AI đọc hướng dẫn" trên màn hình thấp — đã thêm `overflow-y: auto`, kiểm chứng lại bằng cách cuộn thật trong dialog.
  - Kiểm thử sống qua UI thật: trang chủ, điều hướng cả 8 view qua thanh tab và link phụ, dialog khẩn cấp với đếm ngược thật (60→45→...), lưu/đọc cài đặt gia đình (bao gồm số ngân hàng mới), trang "Các hình thức lừa đảo", lịch sử vẫn hiển thị đúng dữ liệu cũ. `npm test` vẫn 32/32 pass sau toàn bộ thay đổi giao diện (bao gồm test contrast tính lại từ bảng màu xanh dương mới).

- 2026-07-21 — **Audit toàn diện + triển khai nhóm bảo vệ cốt lõi B/E/G/F** theo yêu cầu audit trước khi sửa code. Audit đầy đủ lưu tại `AUDIT_2026-07-21.md`. Đã xác nhận với người dùng: còn 6 ngày, ưu tiên đúng 4 nhóm này, không đụng Android/backend-tài khoản (chưa khả thi, đã ghi rõ trong audit).
  - **B — Bộ câu hỏi xác minh** (`#xac-minh`): 9 danh mục, mỗi danh mục có câu hỏi chung + riêng, đọc to bằng TTS, 2 nút kết quả (ổn / né tránh-thúc ép) + nút "người gọi không cho xác minh" — cả hai đều tái dùng cơ chế khẩn cấp của mục E. Không gọi AI, tức thời, dùng được ngay trong cuộc gọi.
  - **E — "Tôi đang bị thúc ép"**: nút khẩn cấp riêng ở trang chủ, tách biệt khỏi dialog tự động sau phân tích (dùng chung 1 dialog, phân biệt qua `data-mode`). Đủ 5 quy tắc rõ ràng, đếm ngược 60 giây thật, câu hỏi "còn đang bị thúc ép không?" sau khi hết giờ, nút chụp/tải ảnh bằng chứng chạy qua `/api/phan-tich` thật.
  - **G — Trợ lý cứu hộ sau chuyển tiền**: câu hỏi mở đầu "Bạn đã chuyển tiền chưa?" dẫn hướng đúng nhánh; checklist 8 bước (thêm bước chuẩn bị mã giao dịch lưu thẳng vào bằng chứng, bước đổi mật khẩu riêng); nút tạo checklist tải về cho người thân; nút sao chép tóm tắt gửi ngân hàng (Clipboard API có fallback tải file — đã test cả 2 nhánh); disclaimer không hứa lấy lại được tiền.
  - **F — Gói bằng chứng mở rộng**: thêm trường ngân hàng/người tự xưng/mã giao dịch/hạn tự xóa (7-30-không xóa); nút điền từ lịch sử; sửa được từng mục; xóa tất cả; disclaimer trung thực "chưa mã hóa" thay vì giả vờ đã mã hóa.
  - Kiểm thử sống toàn bộ qua UI thật: escalate từ B → dialog pressure; đếm ngược + follow-up; điền-sửa-xóa trong F; nhánh rồi/chưa trong G; form nhanh G lưu thẳng vào F (xác nhận qua localStorage); nút copy rơi đúng nhánh fallback khi clipboard bị chặn. `npm test` vẫn 32/32 pass.

- 2026-07-21 — **Vòng 2: A (sổ liên hệ đa người), I (Privacy Center), H (trợ lý báo cáo)**, tiếp tục sau vòng B/E/G/F theo yêu cầu "triển khai đi".
  - **A**: Thay 1 trường "Số điện thoại người thân" bằng sổ liên hệ tối đa 5 người (tên, vai trò, số), mỗi người có nút "Gọi" riêng, có migration tự động từ số cũ đã lưu (không mất dữ liệu). `callFamily()` dùng liên hệ đầu danh sách làm mặc định. Hotline quản trị tập trung — không làm, cần backend, đã ghi rõ trong audit.
  - **I — Trung tâm quyền riêng tư** (`#quyen-rieng-tu`): liệt kê số liệu thật đang lưu, công tắc tắt-lưu-lịch-sử (`addHistory()` no-op khi bật), công tắc cho phép cá nhân hóa (hạ tầng cho D sau này, mặc định tắt), nút tải toàn bộ dữ liệu JSON (không kèm mật khẩu), nút xóa tất cả. Thêm xóa từng mục lịch sử (trước chỉ xóa được tất cả).
  - **H — Trợ lý báo cáo sự việc** (`#bao-cao`): chọn loại sự việc → thông tin đối tượng → thiệt hại → chọn bằng chứng thật từ nhật ký để đính kèm → tóm tắt tự sinh theo thời gian thực → link tinnhiemmang.vn (đã xác minh thật) + gợi ý 113 → sao chép/tải tóm tắt → checkbox tự xác nhận đã gửi (không tự xưng đã gửi thay người dùng). Phân biệt rõ "chuẩn bị nội dung" vs "đã gửi".
  - Kiểm thử sống: migration số cũ; thêm/xóa liên hệ giới hạn 5; toggle tắt lưu lịch sử hoạt động đúng; xuất/xóa toàn bộ dữ liệu; wizard báo cáo cập nhật tóm tắt real-time, tích bằng chứng thật đúng, copy/tải không lỗi. `npm test` vẫn 32/32 pass.

- 2026-07-21/22 — **Giai đoạn 2: 4 tính năng đột phá** theo đề xuất của người dùng (dựa trên nghiên cứu 2026 về scam nhiều lượt và recovery scam), đã tự chọn thứ tự và xác nhận qua AskUserQuestion: còn 4-5 ngày, làm cả 4, ưu tiên deterministic (luật cứng) thay vì gọi thêm Gemini cho phần dự đoán, và thêm tab "Vụ việc" vào thanh điều hướng dưới.
  1. **Scam Journey — "Vụ việc"** (`#hanh-trinh`, tab thứ 5): ghép nhiều diễn biến (cuộc gọi/tin nhắn/link/yêu cầu cài app/chuyển thử/chuyển thêm/giữ bí mật) vào 1 vụ việc, mỗi diễn biến tái dùng `/api/phan-tich` sẵn có để lấy tín hiệu. Endpoint mới `POST /api/phan-tich-hanh-trinh` **không gọi Gemini** — module thuần `src/journey-engine.js` suy ra giai đoạn thao túng hiện tại (làm quen → tạo niềm tin → gây sợ hãi → cô lập → yêu cầu chuyển tiền) và dự đoán bước tiếp theo hoàn toàn bằng luật, tái dùng các tín hiệu boolean đã có sẵn từ Gemini. 8 test mới (`journey-engine.test.js`).
  2. **Dừng thông minh**: nâng cấp `#dangerDialog` — thêm bước tương tác chủ động (chọn câu đang tạo áp lực: "chuyển ngay", "giữ bí mật", "đọc OTP") ngay trong lúc đếm ngược 60 giây thật (không chặn/khoá đếm ngược). Nếu có vụ việc đang xem, tự động ghi nhận lựa chọn thành 1 diễn biến mới khi đóng dialog — nối liền tính năng 1 và 2.
  3. **Lá chắn link/QR** (`#kiem-tra-lien-ket`): giải mã QR hoàn toàn phía trình duyệt bằng `jsqr` (không nén ảnh trước khi giải mã, khác với luồng phân tích chính, để giữ độ phân giải). Backend mới `src/link-shield.js` + `POST /api/kiem-tra-lien-ket`: tự theo dõi chuỗi chuyển hướng URL (lần đầu server gọi ra ngoài tới địa chỉ do người dùng đưa vào) — có chặn SSRF đầy đủ (chỉ http/https, chặn IP nội bộ/loopback/link-local qua `dns.lookup`, giới hạn 5 lần chuyển hướng, timeout 6 giây/lần), rồi so tên miền cuối với thư viện tĩnh domain chính thức của các thương hiệu hay bị giả mạo — hoàn toàn bằng luật, không gọi Gemini. 12 test mới (`link-shield.test.js`) bao gồm test chặn IP nội bộ và mock `fetchImpl`/`dnsLookup` để không cần mạng thật khi `npm test`.
  4. **Lá chắn phục hồi 72h**: kích hoạt từ cuối luồng cứu hộ sau chuyển tiền hoặc thủ công tại Trung tâm quyền riêng tư (72 giờ / 7 ngày). Khi đang bật: banner trên trang chủ hiện thời gian còn lại, checklist tĩnh (đổi mật khẩu/khoá thẻ/kiểm tra giao dịch) lưu tiến độ, nút kết thúc sớm. `/api/phan-tich` nhận thêm cờ tuỳ chọn `che_do_phuc_hoi` (client tự khai) — khi bật, hàm thuần mới `applyRecoveryBoost()` trong `rule-engine.js` quét từ khoá lừa đảo "hỗ trợ lấy lại tiền" (hoàn tiền/thu hồi/luật sư/phí xử lý hồ sơ...) và nâng mức rủi ro tối thiểu lên "Nghi ngờ". Ghi rõ trong copy: chỉ hoạt động khi đang mở ứng dụng, không có thông báo nền. 4 test mới trong `rule-engine.test.js`.
  - **Cắt bớt có chủ đích so với đề xuất gốc** (đã xác nhận với người dùng): Scam Journey và so khớp domain không dùng Gemini thêm lần nào (luật cứng, tái dùng tín hiệu sẵn có); Lá chắn link/QR không chụp/phân tích ảnh xem trước trang đích, không phát hiện ô đăng nhập/OTP trên trang đích (chỉ dừng ở theo dõi chuyển hướng + so tên miền) — ghi rõ là "chưa làm" chứ không giả vờ có.
  - Kiểm thử sống qua UI thật: tạo vụ việc, thêm diễn biến, bấm "Phân tích hành trình" ra đúng giai đoạn/dự đoán; mở dialog khẩn cấp, chọn chip áp lực, xác nhận đếm ngược vẫn chạy độc lập (60→49 giờ thực đo qua nhiều lượt gọi công cụ); dán link thật (google.com) ra "An toàn thấp" đúng chuỗi chuyển hướng; test SSRF chặn `http://127.0.0.1` và `javascript:` đúng; bật/tắt lá chắn phục hồi qua UI thật, tick checklist persist qua localStorage, "Kết thúc sớm" xoá đúng. Xác nhận thanh điều hướng 5 tab vẫn vừa khít ở 375px (phải giảm cỡ chữ 0.72rem→0.66rem và thêm `white-space: nowrap` vì "Trang chủ" bị xuống dòng khi thêm tab thứ 5). `npm test` 56/56 pass.

- 2026-07-22 — **Mở rộng nhận diện từ tài liệu thật (100 tình huống lừa đảo) + câu mở đầu đồng cảm**, theo yêu cầu người dùng ("có cần train con AI này không" → xác nhận không cần fine-tune, chỉ few-shot/thêm luật; "làm cho phần trả lời của AI gần người hơn").
  - Đọc toàn bộ tài liệu `100_tinh_huong_lua_dao.docx` (không có pandoc trong môi trường, phải tự giải nén `.docx` và trích XML bằng Python). Phát hiện 6 kiểu lừa đảo thật chưa có tín hiệu nhận diện: **yêu cầu chia sẻ màn hình/điều khiển từ xa** (rất nguy hiểm, trước đây chỉ có trong danh sách quy tắc tĩnh của dialog khẩn cấp chứ chưa từng là tín hiệu thật), giả danh điện/nước/thuế/phường/bưu điện, đầu tư lợi nhuận cao cam kết không lỗ, người quen qua mạng chưa gặp mặt xin tiền, người thứ ba (giáo viên/bệnh viện/công an) báo tin người thân gặp nạn, đe dọa bằng hình ảnh/video riêng tư (tống tiền). Thêm 6 signal mới vào `SIGNAL_RULES`, cập nhật `SYSTEM_INSTRUCTION` của Gemini, thêm 6 fixture mới lấy nguyên từ tình huống thật trong tài liệu (`test/scenarios.json`), cập nhật danh sách trích dẫn cho phép. `npm test` 63/63 pass.
  - **Câu mở đầu đồng cảm** (`loi_dong_cam`): sau khi hỏi ý kiến người dùng về mức độ AI được viết tự do (chỉ 1 câu đồng cảm an toàn nhất, thay vì diễn đạt lại toàn bộ kết quả), thêm trường mới vào schema Gemini hiện có (không gọi thêm API) — Gemini viết đúng 1 câu ngắn thể hiện thấu hiểu cảm xúc, **viết TRƯỚC khi biết kết luận rủi ro** nên không thể mâu thuẫn với kết quả sau đó. Giới hạn 300 ký tự, mặc định rỗng nếu thiếu/lỗi (không chặn luồng chính nếu Gemini không trả về). Hiển thị phía trên khối kết quả màu trong `analysisView`, và được đọc trước khi đọc kết quả bằng TTS. Kiểm thử sống: 1 tình huống nguy hiểm cao (chia sẻ màn hình) và 1 tình huống an toàn — xác nhận câu đồng cảm giữ đúng giọng trung lập, không tiết lộ/mâu thuẫn kết luận ở cả hai trường hợp. `npm test` vẫn 63/63 pass.

- 2026-07-22 — **Redesign trang chủ theo ảnh tham chiếu 941×1672**: dùng studied-DNA App Hub / Command Center; crop hero ông bà, logo, avatar và khiên khóa từ ảnh người dùng thành WebP cục bộ; đổi font sang Nunito Sans tiếng Việt; chuyển token sang `tokens.css`; nối CTA giọng nói với luồng mic hiện có; giữ nguyên toàn bộ route/backend/tính năng. Đo trình duyệt: hero 901×388, cột 578/301 px, sidebar cao 580 px, khẩn cấp y=1062, banner cuối y=1304, nav 794 px. Responsive 320/375/414/768/941/1280/1440 không tràn ngang; `npm test` 64/64 pass, npm audit 0 lỗ hổng.
- 2026-07-22 — **Làm mượt chuyển màn hình**: giới hạn View Transition vào vùng nội dung thay vì chụp toàn bộ trang; header và thanh điều hướng đứng yên; thêm hướng tiến/lùi, nhịp thoát 170 ms/vào 340 ms, hủy transition cũ khi bấm nhanh, fallback Web Animations và tắt motion theo `prefers-reduced-motion`. Kiểm tra trình duyệt xác nhận đúng animation/easing, không tràn ngang và console sạch; `npm test` 65/65 pass.
- 2026-07-22 — **Đối chiếu brief sản phẩm và bổ sung khoảng trống MVP**: thêm bản đồ thao túng tâm lý deterministic vào kết quả tình huống/chuyển khoản; thêm view `#thoat-cuoc-goi` với 3 câu nói, TTS, gọi người thân/ngân hàng, lưu bằng chứng và nối Vụ việc; nối trực tiếp từ kết quả và dialog khẩn cấp. Tạo `BRIEF_ALIGNMENT_2026-07-22.md` để phân biệt phần đã có, cục bộ và phần cần backend/native. Sửa rail desktop thấp để không che màn hình công cụ; `npm test` 65/65 pass.
