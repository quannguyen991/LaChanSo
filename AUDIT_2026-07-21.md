# Audit Khoan Đã — 2026-07-21

Đọc trực tiếp: `server.js`, `src/rule-engine.js`, `src/gemini.js`, `public/index.html`, `public/app.js`, `public/styles.css`, `test/*`, `package.json`. Không có thư mục Android/Capacitor, không có ORM/DB driver, không có thư viện auth/JWT/bcrypt trong `package.json` — xác nhận đây là **Node/Express phục vụ static + 2 endpoint gọi Gemini + toàn bộ state phía người dùng nằm trong `localStorage` của trình duyệt**. Không có bất kỳ tài khoản, đồng bộ nhiều thiết bị, hay backend lưu trữ nào tồn tại.

## A. Bảng đối chiếu hiện trạng

| Tính năng | Trạng thái | File liên quan | Vấn đề | Cần sửa? |
|---|---|---|---|---|
| Phân tích tình huống (văn bản/giọng nói/ảnh) | Hoạt động đầy đủ | `server.js:30-74`, `src/gemini.js`, `src/rule-engine.js`, `app.js:361-398` | Không | Không |
| Kiểm tra trước khi chuyển tiền | Hoạt động đầy đủ | `server.js:76-113`, `app.js:426-467` | Không | Không |
| Đếm ngược 60s + gọi người thân/ngân hàng khi rủi ro cao | Hoạt động đầy đủ | `app.js:300-347`, `index.html:561-587` | Chỉ kích hoạt **sau khi phân tích ra "Nguy hiểm cao"**, không phải nút khẩn cấp người dùng tự bấm bất kỳ lúc nào (khác với mục E) | Không (nhưng đừng nhầm là đã làm xong mục E) |
| Checklist "vừa chuyển tiền cho người lạ" (6 bước tĩnh) | Có nhưng chưa hoàn thiện | `index.html:355-422` | Thiếu so với 8 bước ở mục G: không hỏi "đã chuyển tiền chưa" trước, không có bước đổi mật khẩu riêng, không có checklist cho người thân hỗ trợ từ xa, không có nút copy tóm tắt gửi ngân hàng | Có |
| Nhật ký bằng chứng | Có nhưng chưa hoàn thiện | `app.js:487-600`, `index.html:424-471` | Chỉ 5 trường (điện thoại/link/tài khoản/thời gian/tóm tắt), thiếu tên ngân hàng, không tự gộp kết quả phân tích AI, không mã hóa, không có hạn tự xóa, không có nút "xóa tất cả" 1 lần | Có |
| Lịch sử kiểm tra (5 gần nhất) | Hoạt động đầy đủ | `app.js:716-757` | Không | Không |
| Cài đặt gia đình (số người thân/ngân hàng/mật khẩu) | Có nhưng chưa hoàn thiện | `app.js:668-710`, `index.html:494-533` | Chỉ hỗ trợ **đúng 1** người thân, không có vai trò, không có danh sách hotline quản trị tập trung | Có |
| Trang "Các hình thức lừa đảo" | Hoạt động đầy đủ nhưng nội dung tĩnh | `index.html:107-135` | Là bài viết cố định, không phải taxonomy quản trị được | Không (chấp nhận được ở quy mô hiện tại) |
| TTS đọc kết quả / STT nhập giọng nói | Hoạt động đầy đủ | `app.js:602-666` | Phụ thuộc trình duyệt hỗ trợ Web Speech API (Chrome/Edge tốt, Safari/Firefox có thông báo fallback) | Không |
| **A. Sổ liên hệ an toàn (đa liên hệ + vai trò + hotline quản trị tập trung)** | Chỉ có 1 phần / phần lớn chưa tồn tại | `app.js:668-710` | 1 số người thân + 1 số ngân hàng đã có; "1-5 người thân có vai trò" và "hotline được quản trị tập trung, cập nhật không cần sửa code" **chưa tồn tại** — vế sau cần backend/CMS thật | Có (phần đa liên hệ); Không khả thi phần hotline tập trung nếu không có backend |
| **B. Bộ câu hỏi xác minh trong cuộc gọi** | Chưa tồn tại | — | Không có bất kỳ danh sách câu hỏi, không có nút "người gọi không cho xác minh" | Có |
| **C. Vaccine lừa đảo (luyện tập)** | Chưa tồn tại | — | Trang cảnh báo hiện tại chỉ là mô tả tĩnh, không phải bài tập tương tác có lựa chọn/phản hồi | Có |
| **D. Bài học cá nhân hóa** | Chưa tồn tại | — | Không có logic đề xuất nào dựa trên lịch sử | Có (rule-based đơn giản) |
| **E. Chế độ "Tôi đang bị thúc ép" (nút khẩn cấp độc lập trên trang chủ)** | Có nhưng chưa hoàn thiện — dễ nhầm với đếm ngược đã có | — | Đếm ngược hiện tại là *hệ quả* của phân tích, không phải nút người dùng tự bấm ngay khi hoảng loạn, trước khi kịp phân tích gì | Có |
| **F. Gói bằng chứng tự động** | Đã audit ở trên | | | Có |
| **G. Trợ lý cứu hộ sau chuyển tiền** | Đã audit ở trên | | | Có |
| **H. Trợ lý báo cáo sự việc (wizard)** | Chưa tồn tại | — | Không có luồng nhiều bước, không phân biệt "chuẩn bị nội dung" / "mở kênh" / "đã gửi" | Có (chỉ ở mức "chuẩn bị nội dung + mở kênh", không tự gửi) |
| **I. Privacy Mode / trung tâm quản lý dữ liệu** | Rải rác, chưa gộp thành 1 trang | `app.js:825-835` (xóa cài đặt gia đình), `historyView` (xóa lịch sử), evidence (xóa từng mục) | Không có trang liệt kê loại dữ liệu/mục đích, không có tải xuống, không có hạn tự xóa, không có "chế độ kiểm tra 1 lần không lưu" | Có |
| **J. Xác nhận hành động nhạy cảm** | Chưa tồn tại | — | App không xử lý giao dịch thật nên không có hành động nào để "chặn" theo đúng nghĩa đen — cần diễn giải lại thành lớp xác nhận trước khi gọi/mở link ngoài | Có, ưu tiên thấp |
| **K. Android Call Shield** | Chưa tồn tại — đúng như dự kiến | — | Codebase 100% web, không có project Android. Theo đúng chỉ dẫn của bạn: **không giả lập, chỉ thiết kế interface/API** | Không code, chỉ tài liệu |
| **L. Accessibility** | Phần lớn đã tốt | `styles.css`, `test/contrast.test.js` | Contrast WCAG 4.5:1 đã tự động test, chữ to, nút to, icon+chữ đi kèm màu, tôn trọng `prefers-reduced-motion`. Thiếu: toggle "chế độ đơn giản hóa" riêng | Có, ưu tiên thấp |
| Mục 3 — không tuyên bố tài khoản chắc chắn lừa đảo | Hoạt động đầy đủ, đã tuân thủ sẵn | `src/gemini.js` (TRANSFER_SYSTEM_INSTRUCTION), `src/rule-engine.js` | Không có câu khẳng định tuyệt đối nào trong code hiện tại | Không |
| Backend: auth, consent, phân quyền, audit log, Family Circle sync, quản lý taxonomy/hotline | **Chưa tồn tại — không có DB, không có bảng nào** | — | Đây là hạ tầng hoàn toàn mới (tài khoản + database + đồng bộ nhiều thiết bị) | **Không khả thi trong kiến trúc/thời gian hiện tại** |

## B. Sẽ triển khai (chỉ mục chưa có/chưa xong, khả thi với kiến trúc hiện tại — Node/Express + localStorage, KHÔNG cần tài khoản/DB)

1. Bộ câu hỏi xác minh theo loại tình huống (B)
2. Sổ liên hệ đa người thân + vai trò, lưu localStorage (A — phần khả thi)
3. Nút khẩn cấp "Tôi đang bị thúc ép" độc lập trên trang chủ (E)
4. Mở rộng gói bằng chứng: thêm trường, liên kết kết quả phân tích, nút xóa tất cả (F)
5. Hoàn thiện trợ lý cứu hộ theo đúng 8 bước + câu hỏi mở đầu (G)
6. Wizard chuẩn bị báo cáo sự việc (H)
7. Trung tâm quyền riêng tư gộp 1 trang (I)
8. Vaccine lừa đảo — luyện tập tương tác (C)
9. Đề xuất bài học theo lịch sử, rule-based (D)

**Không triển khai ngay:** K (Android — chỉ ghi tài liệu kiến trúc), J (ưu tiên thấp, để cuối), hotline quản trị tập trung trong A (cần backend thật), toàn bộ Backend Section 4 (tài khoản/DB/Family Circle sync/audit log).

## Vấn đề mấu chốt cần bạn quyết định trước khi tôi code

Danh sách 9 mục trên — mỗi mục làm **đúng nghĩa** (luồng thật, validation, empty state, accessibility, test) theo tiêu chí bạn đặt ra — là khối lượng công việc lớn hơn nhiều so với những gì đã làm trong các phiên trước (kể cả cộng dồn). Tôi cần biết **còn bao nhiêu thời gian thực tế tới hạn nộp** để chọn đúng tập con làm cho tử tế, thay vì làm dở 9 thứ.
